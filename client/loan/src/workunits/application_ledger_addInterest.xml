<workunit>
    <invokers>
        <invoker type="application:addInterest" caption="Addional Interest" target="popup"/>

        <invoker type="formActions" caption="Add"    mnemonic="a" action="add" default="true"/>
        <invoker type="formActions" caption="Cancel" mnemonic="c" action="_close" immediate="true"/>
    </invokers>
    <code>
        <![CDATA[
        import com.rameses.rcp.annotations.*;
        import com.rameses.rcp.common.*;
        import com.rameses.osiris2.client.*;
        import com.rameses.util.*;                
        
        public class LoanAppLedgerAddInterestController{
            @Service('LoanPaymentService')
            def svc;
            
            def appid;
            def entity = [:];
            def handler;
            
            def add(){
                if(!MsgBox.confirm("Are you sure you want to add this +" +entity.interest+ " day(s) \nof interest on this account. proceed?"))return;
                else
                    MsgBox.alert("Account has been successfully added of +" +entity.interest+ " day(s) of interest.");
                    return '_close';
                
                //entity.appid = appid;
                //entity.mode = 'CAPTURE';
                //svc.postPayment( entity );
                //if( handler ) handler( entity );
            } 
        }
       ]]>
    </code>
    <pages>
        <page template="loan.ui.ledger.LedgerAddInterestPage"/>
    </pages>
</workunit>