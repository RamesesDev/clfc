"context.(address1|address2|state|province|city|zipcode|country)" [properties.readonly == true] { 
    readonly: true;
}

"context.(address1|address2|state|province|city|zipcode|country)" [!properties.readonly] { 
    readonly: false;
}

"context.(address1|state|city|zipcode)" [properties.required] { 
    required: true;
}

"context.(address1|state|city|zipcode)" [!properties.required] { 
    required: false;
}
